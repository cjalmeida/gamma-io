# Sample partial config file with 'datasets' entries
#

datasets:
  group1:

    # a local CSV
    ds1:
      location: file:///tmp/foo.csv
      format: csv

    # a local CSV with options
    ds2:
      location: file:///tmp/foo.csv
      format:
        kind: csv
        sep: "\t"

